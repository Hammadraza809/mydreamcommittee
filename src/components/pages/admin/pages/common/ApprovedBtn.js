import React from 'react';
import { Buttton } from '@material-ui/core';

function ApprovedBtn(id) {
    return (
        <div>
            {console.log(id.props)}
        </div>
    )
}
export default ApprovedBtn;